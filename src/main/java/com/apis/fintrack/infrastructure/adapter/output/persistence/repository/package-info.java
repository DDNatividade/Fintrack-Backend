/**
 * Repositorios de Persistencia.
 * 
 * Contiene:
 * - Interfaces Spring Data JPA (UserJpaRepository, etc.)
 * - Adapters que implementan los puertos del dominio
 *   (UserRepositoryAdapter implements UserRepositoryPort)
 */
package com.apis.fintrack.infrastructure.adapter.output.persistence.repository;

